meta {
  name: 06 - DELETE School
  type: http
  seq: 10
}

delete {
  url: {{resourceBaseUrl}}/ed-fi/schools/{{tempSchoolUniqueId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{edFiCertToken}}
}

assert {
  res.status: eq 204
}

script:post-response {
  if (res.status === 204) {
    console.log('School "{{tempSchoolUniqueId}}" was deleted correctly.');
  } else {
    console.log('There was an error deleting School "{{tempSchoolUniqueId}}" response:');
    console.log(res);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
