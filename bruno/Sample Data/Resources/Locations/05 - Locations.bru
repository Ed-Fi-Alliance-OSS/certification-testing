meta {
  name: 05 - Locations
  type: http
  seq: 5
}

delete {
  url: {{resourceBaseUrl}}/ed-fi/locations/{{tempLocationUUID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{edFiCertToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
