meta {
  name: 05 - Contacts
  type: http
  seq: 5
}

delete {
  url: {{resourceBaseUrl}}/ed-fi/contacts/{{tempContactUUID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{edFiCertToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
