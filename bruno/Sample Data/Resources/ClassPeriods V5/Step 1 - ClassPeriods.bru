meta {
  name: Step 1 - ClassPeriods
  type: http
  seq: 6
}

post {
  url: {{resourceBaseUrl}}/ed-fi/classPeriods
  body: json
  auth: bearer
}

auth:bearer {
  token: {{edFiCertToken}}
}

body:json {
  {
    "classPeriodName": "Class Period 01",
    "schoolReference": {
      "schoolId": 9999999901
    },
    "meetingTimes": [
      {
        "startTime": "08:15:00",
        "endTime": "09:25:00"
      }
    ]
  }
}

script:post-response {
  const locationHeader = res.headers.Location || res.headers.location;
  if (locationHeader) {
    const _UUID = locationHeader.split('/').pop();
    bru.setVar('tempFirstClassPeriodUUID', _UUID);
    console.log('ClassPeriod data was created correctly.');
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
