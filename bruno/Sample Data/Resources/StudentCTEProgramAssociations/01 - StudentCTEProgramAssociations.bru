meta {
  name: 01 - StudentCTEProgramAssociations
  type: http
  seq: 1
}

get {
  url: {{resourceBaseUrl}}/ed-fi/studentCTEProgramAssociations?studentUniqueId=[ENTER_STUDENT_UNIQUE_ID]&programName=[ENTER_PROGRAM_NAME]&programTypeDescriptor=[ENTER_PROGRAM_TYPE]&educationOrganizationId=[ENTER_EDORG_ID]&beginDate=[ENTER_BEGIN_DATE]
  body: none
  auth: bearer
}

params:query {
  studentUniqueId: [ENTER_STUDENT_UNIQUE_ID]
  programName: [ENTER_PROGRAM_NAME]
  programTypeDescriptor: [ENTER_PROGRAM_TYPE]
  educationOrganizationId: [ENTER_EDORG_ID]
  beginDate: [ENTER_BEGIN_DATE]
}

auth:bearer {
  token: {{edFiCertToken}}
}

script:post-response {
  const { setVar, setVarsMessage, wipeVar, wipeVarsWarning, pickSingle } = require('./utils');
  const item = pickSingle(res.getBody());

  if (item) {
    setVar(bru, 'tempStudentCTEProgAssocUUID', item.id);
    setVarsMessage('StudentCTEProgAssociation');
  } else {
    wipeVar(bru, 'tempStudentCTEProgAssocUUID');
    wipeVarsWarning('StudentCTEProgAssociation');
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
