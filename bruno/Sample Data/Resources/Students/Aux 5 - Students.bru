meta {
  name: Aux 5 - Students
  type: http
  seq: 5
}

delete {
  url: {{resourceBaseUrl}}/ed-fi/students/{{tempStudentUUID}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{edFiCertToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
