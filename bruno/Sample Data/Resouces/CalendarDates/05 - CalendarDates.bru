meta {
  name: 05 - CalendarDates
  type: http
  seq: 5
}

delete {
  url: {{resourceBaseUrl}}/ed-fi/CalendarDates/{{tempCalendarDateUniqueId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{edFiCertToken}}
}

script:post-response {
  if(res.status == 204){
    bru.setEnvVar('tempCalendarDateUniqueId', null);
  
    console.warn('Calendar Date data was wiped.')
  }
}

settings {
  encodeUrl: true
}
